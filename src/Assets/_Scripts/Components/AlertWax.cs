using UnityEngine;
using System.Collections;

public class AlertWax : MonoBehaviour {

	// Use this for initialization
	void Start () {
        TimeManager.manager.UpdateDisplay();
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
