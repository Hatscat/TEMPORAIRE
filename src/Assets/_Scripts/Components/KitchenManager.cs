using UnityEngine;
using System.Collections;

public class KitchenManager : MonoBehaviour {

	// Use this for initialization
	void Start () {
        TimeManager.manager.TimerStart();
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
