using UnityEngine;
using System.Collections;

public class HudManagerNGUI : HudManager
{
    /*
    public UILabel labelPlayerName;
    public UILabel labelScoreName;
    public UILabel labelBestScoreName;
    public UILabel labelMsgBotRightName;
    public UILabel labelMsgCenterName;

    new void OnGUI()
    {
    }

    void Update()
    {
        if (!PlayersManager.manager.Player.Equals(labelPlayerName.text))
        {
            labelPlayerName.text = PlayersManager.manager.Player;
        }
    }

    protected override void RefreshDisplayedScore(int score, int bestScore)
    {
        Debug.Log("RefreshDisplayedScore");
        base.RefreshDisplayedScore(score, bestScore);
        //_displayedScore = score;
        //_displayedBestScore = bestScore;
    }
    */
}
